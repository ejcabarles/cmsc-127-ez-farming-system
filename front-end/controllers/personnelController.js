'use strict';
(function() {
    angular
        .module('app')
        .controller('personnelController', personnelController);

        function personnelController($http) {

            var vm = this;
            
        }


})();